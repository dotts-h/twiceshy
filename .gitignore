coverage.out
/twiceshy
.twiceshy-loop.lock

# scratch / build artifacts (ops experiments, local DBs, compiled bins)
.tmp/
/compare
scratch_thinkoff/
__pycache__/
runs/*.log

# derived SQLite index from a test/build run in the repo root
/twiceshy.db
