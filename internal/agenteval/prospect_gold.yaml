# SPDX-License-Identifier: AGPL-3.0-only
#
# Prospector gold cases (#0114): tasks whose OFF arm hit a validated corpus
# trap, generated by `twiceshy prospect -gold-out`. Appended by
# MergeProspectGold (dedup by trap_id, existing entries win); LoadProspectGold
# reads this embedded file. Empty (no cases yet) is a valid, tolerated state.
cases: []
