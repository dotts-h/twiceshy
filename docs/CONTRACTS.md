# CONTRACTS.md — twiceshy stable promises

> The registry of seams: interfaces, routes, event vocabularies, schemas, and
> invariants other code (or other repos) relies on. Changing anything listed
> here is **deliberate** — it gets a decision record and a coordinated rollout,
> never a drive-by edit. This is an *index*, not a prose copy: each entry states
> the shape tersely and points at the source of truth.

## Internal seams

> Promises between modules inside this repo. Changing any of these is deliberate
> ([ADR-0005](adr/ADR-0005-stable-seams.md)) — an ADR and a coordinated rollout,
> never a drive-by edit.

| seam | shape (one line) | stability | owner / source |
|------|------------------|-----------|----------------|
| `search_experience` | `SearchArgs` → `SearchResult` + enveloped text `Content`; ≤ `MaxK` (=3) hits; hit `title`/`summary` transport-sanitized + capped; multi-card body injection-framed; **empty is an answer** (no near-miss padding); quarantined hidden unless `include_quarantined` | stable | `internal/server` ([ADR-0001 §5](adr/ADR-0001-architecture.md)) |
| `get_experience` | `GetArgs{id}` → `GetResult` + same enveloped text `Content`; `GetResult.markdown` is the injection-framed rendering (enveloped + transport-sanitized + capped), not raw store bytes — any status, explicit pull | stable | `internal/server` ([ADR-0001 §5](adr/ADR-0001-architecture.md)) |
| `record_experience` | `RecordArgs` → `RecordResult`; returns a **quarantined** draft to PR or a `known`-duplicate verdict; never writes, never returns a validated record | stable | `internal/server` ([ADR-0001 §6](adr/ADR-0001-architecture.md)) |
| `report_outcome` | `ReportArgs{record_id, outcome, evidence, author}` → `ReportResult`; the disputed record must exist; returns a **quarantined counter-record** (a `dead-end` carrying `provenance.disputes`) to PR — **propose-only**: never mutates the disputed record, never writes, never demotes. Evidence is content-screened like any ingest; a bare report (no evidence) is a triage flag. The #0032 gate adjudicates. An alpha `tok_` tenant gets the full write-path posture: forced `alpha:<token_id>` origin stamping (caller `author` survives only as a display note in the evidence), tighter size caps, fail-closed secret rejection (never quarantine), and a 25/day contribution quota ([ADR-0031](adr/ADR-0031-alpha-write-path-policy-is-a-single-seam.md)) | stable | `internal/server`, `internal/ingest` ([ADR-0013 §3](adr/ADR-0013-closed-loop-autonomous-validation.md)) |
| `report_issue` | `IssueArgs{title, description, category: bug\|feature\|question, related_record_id?, author}` → `IssueResult{category, markdown?, message}`; the surface for **half-formed** input record_experience/report_outcome reject. Title `%q`-quoted into YAML (no injection); content-screened (secrets/PII/harmful) like any ingest. `issue-queue` set → `spool.EnqueueIssue` for `intake-issues`; unset → returns a PR-ready docs/issues markdown. **Triage-flagged, never auto-actioned**; writes no docs/issues file directly. An alpha `tok_` tenant gets the full write-path posture: forced `alpha:<token_id>` origin stamping (caller `author` survives only as a display note in the description), a tighter description size cap, fail-closed secret rejection (never quarantine), and a 25/day contribution quota ([ADR-0031](adr/ADR-0031-alpha-write-path-policy-is-a-single-seam.md)) | stable | `internal/server`, `internal/spool` (#0066) |
| `confirm_helpful` | `ConfirmArgs{id}` → acks; increments `provenance.usage.confirmed_helpful` via `index.ConfirmHelpful` — a **reinforcement signal only**, never edits/mutates the record content or its status. An alpha `tok_` tenant is gated by a 50/day contribution quota (no stamping/caps — it stamps no provenance); the operator tenant is exempt ([ADR-0031](adr/ADR-0031-alpha-write-path-policy-is-a-single-seam.md)) | stable | `internal/server` ([ADR-0013 §4](adr/ADR-0013-closed-loop-autonomous-validation.md)) |
| `POST /push` | `PushArgs{query, ecosystem?, package?, session?, trigger?}` (the hook's query context; `trigger` is `""`/`"prompt"`/`"error"`, any other value 400s) → `PushResult` — the dominant injection channel (`additionalContext`, embedding-free): fingerprint-bypass, or the discriminative-term gate over the **push-eligible subset** (validated, kind `trap`/`fix`, non-importer origin — #0107) with **two-token corroboration** for a prompt-triggered query (`trigger` != `"error"`, #0108; a single discriminative token, or two tokens that don't co-occur on one eligible record, serve nothing) ([ADR-0015](adr/ADR-0015-push-discriminative-term-gate.md), [ADR-0017](adr/ADR-0017-global-idf-push-gate-specificity.md), ADR-0028) decides whether anything is served; quarantined records never reach it; every decision is traced to `telemetry.Record` (#0067) | stable | `internal/server` ([ADR-0001 §5](adr/ADR-0001-architecture.md)) |
| `POST /retro` (session-retro capture) | `RetroArgs{transcript, author?, session?, reason?}` → `RetroResult{queued, status}` (202); **raw HTTP, not MCP** (a shell hook can't speak streamable HTTP), behind the same bearer + rate-limit + 256 KiB body cap. **Screens at the edge**: a `secret`-category finding is refused (422) and never spooled; otherwise `spool.EnqueueTranscript`. `retro-queue` unset → 503 (opt-in). Registered **unqualified** so a non-POST is a clean 405, never an MCP catch-all fall-through (exp-0006). The expensive off-pool analysis is off the request path (the `retro-intake` driver). **Operator-only in the alpha**: an alpha `tok_` tenant gets 403 before any body read/screen/spool work ([ADR-0031](adr/ADR-0031-alpha-write-path-policy-is-a-single-seam.md)) | stable | `internal/server`, `internal/spool` ([ADR-0018](adr/ADR-0018-session-retro-capture.md)) |
| `POST /signup` | `SignupArgs{email, accept_terms}` → `SignupResult{token, quota_per_day, rate_per_min}`; public ONLY when `SignupEnabled`, else 404 indistinguishable from unwired; behind the shared global rate limiter + its own per-IP daily cap (3/day, trusted-proxy XFF resolution per #0131) + timeout/body caps ([ADR-0033](adr/ADR-0033-declared-middleware-pipeline.md)); email normalized (lowercase, single `@`, dotted domain), no verification email; the full token appears ONCE in the response — the store holds only a sha256 of the secret | stable | `internal/server` (#0127, [ADR-0030](adr/ADR-0030-public-alpha-hosted-multitenant-mcp.md)) |
| `GET /statz` | operator-only stats dashboard: any `tok_` tenant gets 403 inside the handler; behind the full authed chain | stable | `internal/server` (#0126) |
| `tenant token format` | `tok_<8 lowercase hex>_<32 lowercase hex>` (id + secret; parse rejects anything else) — an EXTERNAL promise: users store these in MCP client configs; issue/revoke/list via the twiceshy token CLI; per-token `daily_quota` (<=0 = unlimited) and `rate_per_min` (<=0 = server default 60) | stable | `internal/index` (#0125) |
| MCP transport/auth (all three) | streamable HTTP (never HTTP+SSE); **bearer required — no unauthenticated mode**; constant-time token compare; token never logged | stable | `internal/server` ([CONVENTIONS.md](CONVENTIONS.md) Security) |
| `index.Index` | methods `Open`/`Close`/`Rebuild`/`Search`/`Retrieve`/`RetrieveFused`/`Assess`/`Get`/`NextID`/`EmbedCorpus`/`RecordHits`/`RecordPushes`/`ConfirmHelpful`/`Usage`/`CountContributionCall`, plus feature-gated `IssuePlannedToken`/`AssignTokenPlan`/`PlanReport`; values `Query`/`Hit`/`Stored`/`Novelty`/`Assessment`; **usage counters** (`RecordHits`→`retrieved`, `RecordPushes`→`pushed`, `ConfirmHelpful`→`confirmed_helpful`, read via `Usage`) are monotonic `+1` deltas, durable state in the db file (derived index + durable TENANT REGISTRY: the `tokens`, `token_usage`, `tenant_usage`, `contribution_usage`, `organizations`, `workspaces`, and `token_entitlements` tables are durable, survive `Rebuild` [guarded by tenant-registry/team-plan tests], and tokens are credentials stored hash-only — deleting the db file on a tenant-serving deployment is an irreversible credential-revocation event, never a cache rebuild; see [ADR-0034](adr/ADR-0034-tenant-registry-is-not-derived-state.md) and [ADR-0035](adr/ADR-0035-feature-gated-team-plan-foundation.md)), written off the hot path (ADR-0013 §4); `contribution_usage` (written by `CountContributionCall`) is likewise durable, survivable state — the write-path contribution quota's own enforcement-owned atomic debit, decoupled from the `tenant_usage` telemetry counter ([ADR-0032](adr/ADR-0032-atomic-fail-closed-contribution-quota-debit.md)); `pushed` is a distinct push-impression counter (a weaker signal than a deliberate pull, so it never touches `retrieved`/`last_hit`); the `usage.pushed` column is an additive in-place migration in `Open`; precedence **fingerprint-exact → lexical → dense**, **`MaxK` hard cap**, relevance floor as index policy — `Retrieve`/`Assess` apply it, `Search` is the floor-free mechanism, `RetrieveFused` is the **pull entry point** (fingerprint precedence + RRF over lexical+dense; falls back to `Search` with no/erroring embedder) ([ADR-0004](adr/ADR-0004-relevance-floor-is-index-policy.md), [ADR-0007](adr/ADR-0007-floor-on-the-read-path.md), [ADR-0009](adr/ADR-0009-dense-retrieval-is-pure-go-cosine.md)); scores positive/higher-is-better, **embedding-free hot path — `Assess`/`Search`/`Retrieve` never embed; only `RetrieveFused` does** | stable | `internal/index` ([ADR-0001 §3–4](adr/ADR-0001-architecture.md)) |
| `index.Embedder` | `Embed(ctx, text) ([]float32, error)`; pull-only, optional, fallback-safe; `OllamaEmbedder` edge is the only impl, a stub drives tests | stable | `internal/index` ([ADR-0009](adr/ADR-0009-dense-retrieval-is-pure-go-cosine.md)) |
| `index.Novelty` | exactly three states `known`/`similar`/`novel`, keyed on the single relevance floor; no score bands this phase | stable | `internal/index` ([ADR-0006](adr/ADR-0006-defer-score-banding.md)) |
| `ingest.Source` | `Name() string` + `Drafts(ctx) ([]Draft, error)`; a license-clean importer adapter emitting quarantined-record drafts; impls `deprecationSource` (go/py), `osvSource` (embedded snapshot) + `OSVLiveSource` (live osv.dev fetch) | stable | `internal/ingest` ([ADR-0003](adr/ADR-0003-corpus-bootstrap-source-scope.md)) |
| `doctor.Doctor` / `EOLSource` | `Doctor.Run(ctx, recs) (Report, error)` — **report-only/delta, never mutates the corpus**; `EOLSource.Cycles(ctx, product)` feeds D2 staleness; impls `Staleness` (D2) + `repro.Revalidator` (D3, execution-validation) | stable | `internal/doctor`, `internal/repro` ([ADR-0010](adr/ADR-0010-doctors-build-d2-defer-the-rest.md), [ADR-0011](adr/ADR-0011-corpus-growth-and-validation-engine.md)) |
| `repro.Broker` | `Run(ctx, Job) (Result, error)` — runs an untrusted repro in an ephemeral gVisor (runsc) container under a **hardcoded** policy (non-root, read-only rootfs, all caps dropped, mem/cpu/pids/wall-clock caps, named volume, two-phase: prepare networked-trusted → execute `--network=none`); screens staged content + guarantees teardown (watchdog + `Reaper`). The record can never weaken the policy | stable | `internal/repro` ([ADR-0011 §4](adr/ADR-0011-corpus-growth-and-validation-engine.md)) |
| ingestion safety gate | `screen.Scan` over every record text field before write; a hit sets `provenance.security_flags` (a flagged record can never become `validated`); default quarantine-with-flag. `screen.ExecutionHazards` is the calibrated subset (secret + harmful-code) the broker refuses to execute | stable | `internal/screen`, `internal/ingest`, `internal/repro` ([SECURITY_ANALYSIS.md](research/SECURITY_ANALYSIS.md) §2) |
| `judge.Judge` | `Judge(ctx, Request{record, attestation, repros, Prose bool}) (Verdict, error)` — the diverse-model verdict layered on a holding attestation (ADR-0013 §1). `Verdict.Approved()` is the **fail-safe chokepoint**: true only when decision is `approve` AND all five checks (`meaning`/`scope`/`usefulness`/`license`/`poison`; `usefulness` added #0110 — would this record change a competent agent's next action?) pass; any error, empty, or garbled response = **not approved**, never a spurious promote. `ModelJudge` is the off-pool frontier edge; the cheap local model and the drafter's own family are **rejected by construction** (anti-monoculture); a `StubJudge` drives tests. `Request.Prose` routes a no-source/no-repro record to the cross-family **prose-class panel prompt** instead of the repro-attestation prompt ([ADR-0020](adr/ADR-0020-prose-class-panel-promotion.md)) | stable | `internal/judge` ([ADR-0013](adr/ADR-0013-closed-loop-autonomous-validation.md)) |
| `retro.Analyzer` | `Analyze(ctx, transcript) ([]Candidate, error)` — extracts candidate experience records from an **untrusted** session transcript (framed as DATA in a `BEGIN/END SESSION TRANSCRIPT` envelope with breakout-neutralization; the analyzer is prompt-injectable). `ModelAnalyzer` is the off-pool **drafter** edge (reuses the judge shim's `{model,prompt,system}` wire; **no model family forbidden** — output is quarantined, the analyzer drafts, never promotes); a `StubAnalyzer` drives tests. An error leaves the transcript **queued** (fail-safe), never read as "no traps". The `retro-intake` driver feeds candidates through `ingest.Prepare` → quarantined drafts (same ladder as the importer) | stable | `internal/retro` ([ADR-0018](adr/ADR-0018-session-retro-capture.md)) |
| `promote.Promoter` | `Promote(ctx, rec) (Outcome, error)` + `Eligible(rec)` — the positive direction of ADR-0013 (#0029): a **holding attestation AND a judge PASS** flip `quarantined → validated`, stamping `provenance.promotion`. Fail-safe: only the execution-provable class is eligible (quarantined + positive repro, not a `disputes` report, not security-flagged); a non-holding attestation, a judge reject, or a judge outage **leaves the record quarantined**. Mutates the record in place only on success; the gate (`Attestor`) is the trust anchor, the judge a secondary filter. Driven by `twiceshy promote` (judge from `TWICESHY_JUDGE_URL`). Two sibling no-repro paths skip the attestor and use a judge **panel** instead, both born-stale-gated and recording panel verdicts in `provenance.promotion.panel`: `EligibleAdvisory`/`promoteAdvisory` ([ADR-0016](adr/ADR-0016-advisory-class-panel-promotion.md)) and `EligibleProse`/`promoteProse` ([ADR-0020](adr/ADR-0020-prose-class-panel-promotion.md)) | stable | `internal/promote` ([ADR-0013 §1–2](adr/ADR-0013-closed-loop-autonomous-validation.md)) |
| `guard.Guardrails` | `{Paused, MaxActions, MaxRuns}` + `Engaged()` / `Budget()` — the safety net around autonomous promote/demote (ADR-0013 §7, #0033): an **emergency stop** (`TWICESHY_PAUSE` halts ALL auto-promotion/demotion, fail-safe), an **anomaly monitor** (a promotion/demotion rate past `MaxActions` alerts — the "judge approving everything" catch; it notifies, the pause halts), and a **budget cap** (`MaxRuns` ceilings the broker/judge runs one invocation can trigger). Consulted by both `promoteCorpus` and `adaptCorpus` | stable | `internal/guard` ([ADR-0013 §7](adr/ADR-0013-closed-loop-autonomous-validation.md)) |
| `promote.Adapter` | `Adapt(ctx, original, report, CounterEvidence, corroborating) (AdaptOutcome, error)` — the negative direction (#0032): given an outcome report + the broker re-run, a **reproduced failure (original broke OR counter reproduced) AND a judge PASS** demote `validated → stale` (`provenance.demotion`); independent **non-reproducing** reports corroborated past `DisputeThreshold` (>1) flip `validated → disputed` (escalate, reversible) — one report can never demote a correct card (the attribution guard); an inconclusive counter is no signal; a judge outage is a fail-safe no-op. Driven by `twiceshy adapt` | stable | `internal/promote` ([ADR-0013 §3](adr/ADR-0013-closed-loop-autonomous-validation.md)) |
| record schema + identity | on-disk record format (`schema_version: 1`, versioned JSON Schema in `schema/`; additive `provenance.source_license`/`source_url`/`security_flags`/`disputes`/`promotion`/`demotion`, the `disputed` status, and the `guard.repros` test-set) and the `exp-NNNN` id `NextID` allocates and the path embeds — the contract between writer (`ingest`/`record`) and reader (`index`) | stable | [SCHEMA.md](SCHEMA.md), `schema/` |

## Provides (consumed by other repos)

> Machine-checked by `fleet-doctor.sh`. Format — one bullet per contract:
> ``- `contract-id` — description · shape/schema pointer``
> The id is fleet-unique, kebab/dot style (e.g. `acme.users.api-v1`).

- *(none yet)*

## Consumes (provided by other repos)

> Same format. Every id listed here must be **provided** by exactly one sibling
> repo in the fleet's `constellation.yaml` — the fleet doctor fails otherwise.

- *(none yet)*

## Invariants

> Cross-cutting promises that aren't a single seam (determinism, ordering,
> escaping, atomicity). Each cites its decision record.

- **Quarantined-only write (the trust boundary).** Agent-proposed records land
  `quarantined`; promotion to `validated` requires the guard's sandbox
  fail-to-pass **plus** human PR review — **the PR is the trust boundary**. No
  code path writes a `validated` record, and quarantined records never reach the
  push channel. ([ADR-0001 §6](adr/ADR-0001-architecture.md),
  [ADR-0005](adr/ADR-0005-stable-seams.md))
- **Relevance floor is index policy, not a per-call argument.** Every novelty
  classification applies `index.DefaultFloor` by default; floor-off is the
  explicit, greppable `index.FloorOff` sentinel, never an accidental zero.
  ([ADR-0004](adr/ADR-0004-relevance-floor-is-index-policy.md))
