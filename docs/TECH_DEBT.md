# TECH_DEBT.md — twiceshy debt register

> Known, *deliberate* debt — each entry names what was traded away, why, and the
> trigger that should force repayment. An entry with no repayment trigger is a wish,
> not a register row. Paying an item down removes the row (git history remembers).

| # | item | why deferred | repay when |
|---|------|--------------|------------|
| M3 | `index.NextID` is a `MAX(id)+1` read, not an atomic allocation — two concurrent `record_experience` calls can be handed the same id | the write path is **propose-only**: it returns a quarantined draft for a human to open as a PR (the trust boundary), and a colliding id is caught in review and cheap to re-derive at merge | concurrent *unattended* writes become possible (auto-merge, or a non-PR write path) — then reserve/allocate the id transactionally or re-derive it at merge |
| L6/L7 | `DefaultFloor` (`2.0e-06`) is a coarse, corpus-coupled BM25 band, pinned only by a *relative* boundary test; a proportional BM25 shift could flip Similar↔Novel while the seed-fixture guard stays green | the seed corpus is tiny; an absolute/normalized band needs more data to calibrate | the corpus outgrows the seed set — replace with normalized/RRF banding (ADR-0006) |
| ADR-0006 | score-banding (explicit Similar-vs-Novel thresholds) is deferred | dense + RRF have now landed (ADR-0009), but no consumer yet needs more than the floor + raw scores | a consumer (e.g. an LLM-arbitrated doctor, ADR-0010 D1) demonstrably needs a calibrated band |
| denseFloor | the dense cosine floor (`0.5`) is a coarse demote-the-noise heuristic, not a calibrated band — the dense-phase analogue of L6/L7 (ADR-0009) | the seed corpus is tiny; a calibrated cosine cutoff needs more data | the corpus grows enough that 0.5 admits weak semantic near-misses — recalibrate or land normalized banding |
| screen/pack policy | the ingestion-gate detector policy (entropy threshold, exec-only harmful-code patterns, quarantine-with-flag default) and the pack license-eligibility matrix live only in package doc-comments + issues, not an ADR | shipped under time pressure; not yet contended | a policy change is proposed (e.g. "is LGPL commercial-safe?", a new detector class) — record it as an ADR (or extend ADR-0002) |
| #0025 | the #0018 broker's `/work` named volume is disk-backed with **no hard size cap**, so untrusted execute-phase code could fill host disk (bounded only by the phase wall-clock) | a tmpfs-backed volume (which would cap size) is re-created empty per mount, so it can't carry the warmed dep cache from prepare → execute; disk-backed is required to share state. Single-tenant on the brain it's a recoverable self-DoS (the volume is removed in cleanup) | twiceshy runs multi-tenant (epic 0010) — then enforce a hard per-run disk quota (loopback ext4 / FS project-quota / disk-usage watchdog). Tracked as #0025 |
| clock seam | `cmd.reapOrphans` (#0052) is a package-level mutable `var` overridden in tests — a tension with CONVENTIONS "no package-level mutable state; dependencies enter through constructors" (`record`'s own clock seam was already fixed via constructor/parameter injection, ADR-0024) | the pragmatic Go test seam for a single free function's wall-clock boundary; never mutated in production | `cmd.reapOrphans` gains a second injected dependency, or its caller grows config — then thread the clock through explicitly the same way `record.Validate` now does |
| off-pool transport | `internal/retro/model.go` `ModelAnalyzer.Analyze` duplicates `internal/judge/model.go`'s off-pool POST spine near-verbatim (marshal `{model,prompt,system}` → POST → status/empty/1 MiB-limit handling), differing only in the decoded response shape (ADR-0018) | unifying means refactoring the stable `judge` seam (#0028, ADR-0013), out of scope for #0065's capture spine; both copies are correct today | a third off-pool caller appears, or a transport hardening change must land in both — then extract a shared leaf POST helper both import (neither imports the other; cycle-free) |
| DATA envelope | the `BEGIN/END … DATA` framing + forged-end-delimiter neutralization + control-char strip is implemented twice: `internal/server/render.go` (`renderEnvelope`) and `internal/retro/analyzer.go` (`frameTranscript`), with different delimiter strings | a shared `frameData(begin,end,body)` leaf helper would touch the stable `server` injection seam (push/get/search), out of scope for #0065; both copies now neutralize the end delimiter **and** strip control chars | the framing rules change again, or a third framer appears — then extract one parameterized leaf helper imported by `server` and `retro` (cycle-free) |
